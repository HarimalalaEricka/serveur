# ---------- Base de données PostgreSQL sur Railway ----------
# Railway fournit DATABASE_URL, DATABASE_USERNAME et DATABASE_PASSWORD
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

# ---------- JPA / Hibernate ----------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ---------- Port du serveur ----------
# Railway fournit la variable d'environnement PORT
server.port=${PORT:8080}

# ---------- Sécurité (optionnel) ----------
# Si tu as besoin d'activer CORS pour ton application Android
# spring.web.cors.allowed-origins=http://ton-ip-ou-domaine
